---
name: stock-analysis-entry
description: 股票全市场分析入口，支持中国 A 股、美股、港股的深度透视。Agent 需根据用户需求自动执行以下链式量化分析流程：1. 市场识别与拦截（目前仅支持 A 股，禁止非 A 股调用 MCP）；2. 财务黑名单筛选（剔除资本结构激进标的）；3. 财务白名单发现（基于 ROE、现金流等指标锁定优质股）；4. 定投组合构建（基于 DCF 模型计算估值折扣并自动加入核心品种）；5. 实时资讯抓取与 LLM 舆情打分。支持用户自定义财务指标，并要求每阶段必须输出详细反馈。
---

## 市场选择与引导 (Market Entry)
当用户提出股票分析需求时，Agent 应首先识别或引导用户确认目标市场。

**支持市场状态**：
- **🇨🇳 中国 A 股**：**[已上线]** 支持全流程量化分析（黑名单/白名单/定投组合/舆情打分）。
- **🇺🇸 美股 (US Stocks)**：[开发中] 暂不支持全流程分析。
- **🇭🇰 港股 (HK Stocks)**：[开发中] 暂不支持全流程分析。

**Agent 行为准则**：
1. **市场识别与拦截**：Agent 应通过股票简称或代码直接识别其所属市场。如果识别结果为非 **中国 A 股**（如美股、港股等），Agent **必须直接回复用户不支持对应的股票分析，且严禁调用 MCP 进行查询**。
2. **引导建议**：对于不支持的市场，Agent 应礼貌告知当前仅支持 **中国 A 股** 的深度量化分析，并询问是否需要对 A 股标的进行操作。
3. **识别执行**：对于确认所属市场的 A 股，Agent 应按照以下深度分析流程执行。

---

## 核心操作流 (Agent 行为准则)
当用户提出分析需求时，Agent 应优先通过 **MCP (`stocks`)** 获取实时数据。

**强制执行与反馈准则**：
1. **参数优先级**：用户指定的指标优先。
2. **主动链式执行与阶段反馈**：Agent **严禁在阶段间停下询问用户是否继续**。必须按照以下 5 个阶段顺序自动、连续地执行。在完成每个阶段后，Agent 应**立即在输出中展示该阶段的详细计算结果与分析报告**，并紧接着开始执行下一个阶段，直至流程全部完成。
3. **阶段顺序**：**计算黑名单 -> 计算白名单 -> 构建定投组合 -> 获取资讯 -> 舆情打分**。报告应呈现清晰的阶段性结构，使用户能够逐步审阅分析过程。

---

### 阶段 1：计算黑名单 (Blacklist Screening)
- **数据范围**: `begin_date`='2023-01-01', `end_date`='2025-12-31' (数据跨度 3-5 年)。
- **排除行业**: `申万一级行业` != '房地产'。
- **默认黑名单标准**:
  - `长期融资净值` < 0 且 `FCF` < 0.2 * abs(长期融资净值)
  - 或 `FCF` < 0
- **阶段反馈要求**：**必须**列出所有被剔除的黑名单股票，并详细解释每只股票触发黑名单的具体财务原因（如：长期融资净值严重不足或 FCF 持续为负）。报告应具备极高的可读性。

### 阶段 2：计算白名单 (Whitelist Screening)
- **数据范围**: `begin_date`='2023-01-01', `end_date`='2025-12-31'。
- **前置条件**: 排除黑名单中的股票。
- **排除行业**: `申万二级行业` 不属于 ['房地产服务', '房地产开发和运营']。
- **默认白名单标准**:
  - `净利润` > 0
  - `FCF` > 0
  - `ROE` > 10%
  - `经营资产息税前收益率 (BIT ROA)` >= 10% (计算公式：EBIT / (净营运资本 + 长期经营资产))
  - `EBIT 经营收益占比` >= 75% (计算公式：营业利润 / (营业利润 + 联营企业和合营企业投资收益 + 投资收益))
  - `经营利润现金含量` > 0.65 (计算公式：经营性现金流净额 / 营业利润)
- **阶段反馈要求**：展示进入白名单的股票列表。**必须**提供一个核心财务指标（ROE、BIT ROA、现金含量等）的对比表格，方便用户直观评估。

### 阶段 3：构建定投组合 (Portfolio Construction)
- **数据范围**: `begin_date`='2023-01-01', `end_date`='2025-12-31'。
- **数据来源**: 从白名单中进一步筛选。
- **DCF 估值模型**:
  - `fcf_base` = 最近一期 FCF。
  - `预测期 (5年) 增长率` = min(30%, 最近增长率)；其中最近增长率计算：`fcf_list.pct_change(4).iloc[-1]`。
  - `永续增长率` = 2.5%, `折现率 (WACC)` = 5%。
  - `企业价值 (EV)` = 预测期折现 FCF 总和 + 永续期折现价值 + 金融资产 + 长期股权投资 - 总负债 - 区间分红。
  - `每股估值` = EV * (1 - 少数股东权益占比) / 总股本。
- **默认筛选标准**:
  - `折扣率` (现价/每股估值) < 1.0。
  - `最近增长率` > 0。
- **组合构建逻辑**:
  - **常规行业**: 每个申万一级行业选 `ROE` 前 3 名。
  - **特殊行业处理**: 金融（银行、非银金融）和房地产行业由于报表结构特殊，不适用上述黑白名单及 DCF 估值逻辑。
  - **强制加入品种 (核心金融与地产资产)**: 
    - 招商银行 (600036.SH)、平安银行 (000001.SZ) [银行]
    - 中国平安 (601318.SH)、中信证券 (600030.SH)、东方财富 (300059.SZ) [非银金融]
    - 万科A (000002.SZ)、保利发展 (600048.SH) [房地产]
- **阶段反馈要求**：展示最终选出的定投组合。**必须**包含行业分布、每股估值、当前折扣率以及强制品种的特别说明。

### 阶段 4：获取资讯与舆情打分 (Sentiment Analysis)
- **获取资讯**: 
  - 针对定投组合中的每只股票，Agent 必须通过运行 Python 脚本获取实时资讯（数据库中不包含新闻/公告数据）。
  - **抓取公告**: 运行 `python references/getAllAnnouns.py <股票代码>` (若 python 不可用，则使用 `python3`)。
  - **抓取新闻**: 运行 `python references/getAllNews.py <股票代码>` (若 python 不可用，则使用 `python3`)。
- **舆情打分**: Agent 作为金融分析师，直接对脚本输出的资讯内容进行深度评估。**打分逻辑应包含**：
  - **打分区间**: -1 (极度利空) 到 1 (极度利好)，0 为中性。分数的绝对值表示影响的大小。
  - **冲突处理**: 若同一时间段内出现矛盾因素（如“大额融资买入”与“大股东减持”同时存在），Agent 必须比较两者的影响力度（如金额大小）给出综合得分。
  - **正向因素**: 业绩预增、大额中标、融资买入创新高、高管增持。
  - **负向因素**: 监管处罚、股东减持、违约、财务造假嫌疑、融资余额新低。
- **阶段反馈要求**：展示每家公司的核心舆情摘要。**必须**提供一个对应的量化打分表格（包含：公司名、打分、评分理由），并解释如何处理了其中的矛盾因素。

---

## 核心财务计算公式 (Logic Reference)
Agent 在编写 SQL 或分析数据时应遵循以下公式：
- **净营运资本 (WC)** = (应收票据及应收账款 + 预付款项 + 存货 + 长期应收款) - (应付票据及应付账款 + 预收款项 + 合同负债 + 应付职工薪酬 + 应交税费)
- **长期经营资产** = 固定资产合计 + 在建工程合计 + 无形资产 + 生产性生物资产 + 油气资产 + 商誉 + 长期待摊费用 + 递延所得税资产 - 递延所得税负债 + 其他非流动资产
- **长期融资净值** = (长期债务 + 所有者权益合计) - 长期经营资产 - 长期股权投资 - 净营运资本
- **自由现金流 (FCF)** = 经营性现金流净额 - (固定资产折旧油气资产折耗生产性生物资产折旧 + 无形资产摊销 + 长期待摊费用摊销) + 研发费用
- **长期债务** = 短期借款 + 长期借款 + 应付债券 + 一年内到期的非流动负债 + 拆入资金 + 交易性金融负债 + 吸收存款及同业存放 + 应付利息

## 数据抽取流程 (Stock Data Flow)
1. **设定时间跨度**: 默认使用 `begin_date`='2023-01-01' 至 `end_date`='2025-12-31' 的财务数据进行趋势分析。
2. **获取标的**: 调用 **MCP (`stocks`)** 查询 `sw_index_stocks` 获取行业分布及代码。
3. **提取财务**: 查询 `asset_balance_sheet`, `stock_profit_sheet`, `cash_flow` 获取历史报表。
4. **计算指标**: Agent 在上下文中计算上述定义的财务指标（重点关注 `长期融资净值` 和 `FCF` 的趋势）。
5. **组合评估**: 结合 `daily_kline` 的收盘价和市值进行估值计算。

## 触发建议
- 当用户询问“哪些股票值得买？”、“分析一下这只股票的财务健康度”或“帮我筛选符合条件的 A 股”时，请调用此技能并按照上述流程执行。
